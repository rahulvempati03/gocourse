package basics

const pi = 3.14
const GRAVITY = 9.81

func main() {

	const days int = 7

	const (
		monday        = 1
		tuesday       = 2
		wednesday     = 3 // untyped constant
		thursday  int = 4 // specifying type explicitly
	)

	name := "Gopher" // name is a variable, not a constant
}
